spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/TanzeemAlam/ecommerce-application-config-repo
server:
  port: 8888

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/